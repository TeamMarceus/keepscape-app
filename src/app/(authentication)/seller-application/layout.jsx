import React from 'react';

import PropTypes from 'prop-types';


export const metadata = {
  title: 'Application | Keepscape Seller',
};

export default function SellerApplicationLayout({ children }) {
  return children;
}

SellerApplicationLayout.propTypes = {
  children: PropTypes.any,
};
