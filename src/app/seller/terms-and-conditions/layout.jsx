import PropTypes from 'prop-types';

export const metadata = {
  title: 'Terms and Conditions | Seller',
};

export default function TermsAndConditionsLayout({ children }) {
  return children;
}

TermsAndConditionsLayout.propTypes = {
  children: PropTypes.any,
};
