import PropTypes from 'prop-types';

export const metadata = {
  title: 'Finance Withdrawals | Seller',
};

export default function FinanceWithdrawalsLayout({ children }) {
  return children;
}

FinanceWithdrawalsLayout.propTypes = {
  children: PropTypes.any,
};
