import PropTypes from 'prop-types';

export const metadata = {
  title: 'Finance Histories | Seller',
};

export default function FinanceHistoriesLayout({ children }) {
  return children;
}

FinanceHistoriesLayout.propTypes = {
  children: PropTypes.any,
};
