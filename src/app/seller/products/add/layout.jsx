import PropTypes from 'prop-types';

export const metadata = {
  title: 'Add Product | Seller',
};

export default function AddProductLayout({ children }) {
  return children;
}

AddProductLayout.propTypes = {
  children: PropTypes.any,
};
