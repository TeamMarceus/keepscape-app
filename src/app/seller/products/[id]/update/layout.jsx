import PropTypes from 'prop-types';

export const metadata = {
  title: 'Update Product | Seller',
};

export default function UpdateProductLayout({ children }) {
  return children;
}

UpdateProductLayout.propTypes = {
  children: PropTypes.any,
};
