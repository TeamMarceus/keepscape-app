import PropTypes from 'prop-types';

export const metadata = {
  title: 'OnGoing Orders | Seller',
};

export default function OnGoingOrdersLayout({ children }) {
  return children;
}

OnGoingOrdersLayout.propTypes = {
  children: PropTypes.any,
};
