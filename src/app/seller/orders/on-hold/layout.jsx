import PropTypes from 'prop-types';

export const metadata = {
  title: 'Onhold Orders | Seller',
};

export default function OnHoldOrdersLayout({ children }) {
  return children;
}

OnHoldOrdersLayout.propTypes = {
  children: PropTypes.any,
};
