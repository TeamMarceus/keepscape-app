import PropTypes from 'prop-types';

export const metadata = {
  title: 'Pending Approvals | Seller',
};

export default function PendingApprovalsLayout({ children }) {
  return children;
}

PendingApprovalsLayout.propTypes = {
  children: PropTypes.any,
};
