import PropTypes from 'prop-types';

export const metadata = {
  title: 'Keepscape Philippines | Search',
};

export default function SearchLayout({ children }) {
  return children;
}

SearchLayout.propTypes = {
  children: PropTypes.any,
};
