import PropTypes from 'prop-types';

export const metadata = {
  title: 'Keepscape Philippines | Province',
};

export default function ProvinceLayout({ children }) {
  return children;
}

ProvinceLayout.propTypes = {
  children: PropTypes.any,
};
