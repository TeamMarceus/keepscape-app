import PropTypes from 'prop-types';

export const metadata = {
  title: 'Questionnaire | Gift Giver',
};

export default function QuestionnaireLayout({ children }) {
  return children;
}

QuestionnaireLayout.propTypes = {
  children: PropTypes.any,
};
