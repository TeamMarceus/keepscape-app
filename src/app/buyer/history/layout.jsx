import PropTypes from 'prop-types';

export const metadata = {
  title: 'History | Gift Giver',
};

export default function HistoryLayout({ children }) {
  return children;
}

HistoryLayout.propTypes = {
  children: PropTypes.any,
};
