import PropTypes from 'prop-types';

export const metadata = {
  title: 'Keepscape Product | Buyer',
};

export default function ProductLayout({ children }) {
  return children;
}

ProductLayout.propTypes = {
  children: PropTypes.any,
};
