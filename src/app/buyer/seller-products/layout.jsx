import PropTypes from 'prop-types';

export const metadata = {
  title: 'Keepscape Seller Products | Buyer',
};

export default function SellerProductsLayout({ children }) {
  return children;
}

SellerProductsLayout.propTypes = {
  children: PropTypes.any,
};
