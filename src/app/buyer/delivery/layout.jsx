import PropTypes from 'prop-types';

export const metadata = {
  title: 'Keepscape Delivery | Buyer',
};

export default function DeliveryLayout({ children }) {
  return children;
}

DeliveryLayout.propTypes = {
  children: PropTypes.any,
};
