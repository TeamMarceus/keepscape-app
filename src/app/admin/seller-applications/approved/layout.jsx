import PropTypes from 'prop-types';

export const metadata = {
  title: 'Approved Sellers Applications | Admin',
};

export default function ApprovedSellersLayout({ children }) {
  return children;
}

ApprovedSellersLayout.propTypes = {
  children: PropTypes.any,
};
