import PropTypes from 'prop-types';

export const metadata = {
  title: 'Pending Seller Applications | Admin',
};

export default function PendingSellersLayout({ children }) {
  return children;
}

PendingSellersLayout.propTypes = {
  children: PropTypes.any,
};
