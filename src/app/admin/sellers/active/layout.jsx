import PropTypes from 'prop-types';

export const metadata = {
  title: 'Active Sellers | Admin',
};

export default function ActiveSellersLayout({ children }) {
  return children;
}

ActiveSellersLayout.propTypes = {
  children: PropTypes.any,
};
