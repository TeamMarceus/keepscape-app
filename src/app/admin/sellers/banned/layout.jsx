import PropTypes from 'prop-types';

export const metadata = {
  title: 'Banned Sellers | Admin',
};

export default function BannedSellersLayout({ children }) {
  return children;
}

BannedSellersLayout.propTypes = {
  children: PropTypes.any,
};
