import PropTypes from 'prop-types';

export const metadata = {
  title: 'Pending Finance | Admin',
};

export default function PendingFinanceLayout({ children }) {
  return children;
}

PendingFinanceLayout.propTypes = {
  children: PropTypes.any,
};
