import PropTypes from 'prop-types';

export const metadata = {
  title: 'Rejected Finance | Admin',
};

export default function RejectedFinanceLayout({ children }) {
  return children;
}

RejectedFinanceLayout.propTypes = {
  children: PropTypes.any,
};
