import PropTypes from 'prop-types';

export const metadata = {
  title: 'Active Buyers | Admin',
};

export default function ActiveBuyersLayout({ children }) {
  return children;
}

ActiveBuyersLayout.propTypes = {
  children: PropTypes.any,
};
