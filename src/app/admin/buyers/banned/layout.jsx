import PropTypes from 'prop-types';

export const metadata = {
  title: 'Banned Buyers | Admin',
};

export default function BannedBuyersLayout({ children }) {
  return children;
}

BannedBuyersLayout.propTypes = {
  children: PropTypes.any,
};
