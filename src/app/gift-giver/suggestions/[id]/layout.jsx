import PropTypes from 'prop-types';

export const metadata = {
  title: 'Suggestions | Gift Giver',
};

export default function SuggestionsLayout({ children }) {
  return children;
}

SuggestionsLayout.propTypes = {
  children: PropTypes.any,
};
